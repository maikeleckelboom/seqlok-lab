\* TLC invariants-only configuration for HotSwapRejectBusy.tla
\* Faster multi-swap runs: safety checks only, no liveness properties.
\*
\* Shares the same constants as the full config so state-space shape matches.

CONSTANTS
    MAX_PREWARM_BLOCKS = 2
    MAX_FADE_FRAMES    = 4
    BLOCK_FRAMES       = 2
    MAX_BEHAVIORS      = 4

    Engine1 = "Engine1"
    Engine2 = "Engine2"
    Engine3 = "Engine3"
    NoEngine = "NoEngine"

SPECIFICATION
    Spec

\* Safety properties over multi-swap histories

INVARIANTS
    TypeOK
    AtMostTwoEngines
    NoGapDuringCrossfade
    NextEngineConsistency
    CompletedSwapsConsistency
    SequentialSwapsComplete

\* Same behavior bound as the full config, but without liveness checks

CONSTRAINT
    BehaviorBound

