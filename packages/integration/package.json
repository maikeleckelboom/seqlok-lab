{
  "name": "@seqlok/integration",
  "version": "0.3.0-next.0",
  "description": "Integration wiring on top of Seqlok core, commands, and hotswap.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "source": "./src/index.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/**/*",
    "docs/HOTSWAP_INTEGRATION.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "vite build && rollup -c rollup.dts.config.mts",
    "clean": "rimraf dist .eslintcache pnpm-debug.log",
    "purge": "pnpm run clean && rimraf node_modules",
    "lint": "eslint src tests",
    "lint:fix": "eslint src tests --fix",
    "format": "prettier --ignore-path ../../.prettierignore --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:types": "pnpm --workspace-root test:types",
    "bench": "vitest bench --reporter=verbose --run",
    "dev": "pnpm run test:watch"
  },
  "dependencies": {
    "@seqlok/base": "workspace:*",
    "@seqlok/primitives": "workspace:*",
    "@seqlok/core": "workspace:*",
    "@seqlok/commands": "workspace:*",
    "@seqlok/hotswap": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "eslint-import-resolver-typescript": "catalog:",
    "eslint-plugin-import": "catalog:",
    "eslint-plugin-regex": "catalog:",
    "fast-check": "catalog:",
    "globals": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "rollup": "catalog:",
    "rollup-plugin-dts": "catalog:",
    "vitest": "catalog:",
    "vite": "catalog:"
  }
}
